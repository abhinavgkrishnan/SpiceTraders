{
  "name": "foundry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "forge build",
    "test": "forge test",
    "deploy:sepolia": "forge script script/FullDeploy.s.sol:FullDeployScript --rpc-url base_sepolia --broadcast --verify -vvvv",
    "deploy:base": "forge script script/FullDeploy.s.sol:FullDeployScript --rpc-url base --broadcast --verify -vvvv"
  }
}
