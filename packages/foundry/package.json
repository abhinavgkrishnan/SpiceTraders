{
  "name": "foundry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "forge build",
    "test": "forge test",
    "deploy:sepolia": "forge script script/Deploy.s.sol:DeployScript --rpc-url base_sepolia --broadcast --verify -vvvv",
    "deploy:base": "forge script script/Deploy.s.sol:DeployScript --rpc-url base --broadcast --verify -vvvv"
  }
}